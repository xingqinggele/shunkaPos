<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_line_bg">

    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="47dp"
        android:text="确定退出当前账号？"
        android:textColor="#ff767676"
        android:textSize="17sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_marginTop="47dp"
        android:layout_marginBottom="32dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dialog_title">

        <RadioButton
            android:id="@+id/dialog_radio_cancel"
            style="@style/dialog_radioButton_style"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            android:background="@drawable/dialog_tab"
            android:checked="true"
            android:text="取消" />

        <RadioButton
            android:id="@+id/dialog_radio_confirm"
            style="@style/dialog_radioButton_style"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            android:layout_marginLeft="48dp"
            android:background="@drawable/dialog_tab"
            android:checked="false"
            android:text="确定" />


    </RadioGroup>
</android.support.constraint.ConstraintLayout>